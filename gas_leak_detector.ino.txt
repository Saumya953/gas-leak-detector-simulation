#define SENSOR A0
#define BUZZER 8
#define LED 7

void setup() {
  pinMode(BUZZER, OUTPUT);
  pinMode(LED, OUTPUT);
}

void loop() {
  int gas = analogRead(SENSOR);

  digitalWrite(BUZZER, gas > 400);
  digitalWrite(LED, gas > 400);

  delay(500);
}
